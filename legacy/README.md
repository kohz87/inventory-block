# Legacy Inventory Block

Inventory Block v0.5.0 is a clean message-native rewrite. The complete pre-rewrite implementation is preserved here for reference.

- `v0.4.3/` contains the final backend/revision architecture, including all source, tests, docs, workflow, manifest, and package metadata from the v0.4.x line and earlier history.
- Legacy code is not loaded by the active extension.
- Do not import legacy modules into the v0.5 runtime; copy ideas deliberately if a feature is reintroduced.
